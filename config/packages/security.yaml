security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            pattern: ^/api
            stateless: true
            provider: app_user_provider
            jwt: ~
            logout:
                path: app_logout

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # Allow public access to login and register endpoints
        - { path: ^/api/sse, roles: PUBLIC_ACCESS }
        - { path: ^/api/performance/health, roles: PUBLIC_ACCESS }
        - { path: ^/api/login, roles: PUBLIC_ACCESS }
        - { path: ^/api/register, roles: PUBLIC_ACCESS }
        # Restrict all other /api routes to authenticated users
        - { path: ^/api, roles: ROLE_USER }
        # Allow public access to everything else (for development)
        - { path: ^/, roles: PUBLIC_ACCESS }
        # DEVELOPMENT MODE: Allow public access to all other routes
        # - { path: ^/, roles: PUBLIC_ACCESS }
        # The following are commented out for development:
        # - { path: ^/api, roles: ROLE_USER }
        # - { path: ^/dashboard, roles: ROLE_USER }
        # - { path: ^/patients, roles: ROLE_USER }
        # - { path: ^/doctors, roles: ROLE_USER }
        # - { path: ^/appointments, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
